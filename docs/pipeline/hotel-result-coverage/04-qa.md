# TEST-HOTEL-RESULT-COVERAGE-01 QA Report

Date: 2026-07-22  
Result: **FAIL**  
Tested revision: `977132b` (`agent/TEST-HOTEL-RESULT-COVERAGE-01`)

## Gate summary

The assigned DEV implementation is not present in the TEST worktree. The TEST branch is at `origin/main` (`977132b`), while the assigned implementation exists only on `agent/DEV-HOTEL-RESULT-COVERAGE-01` at `0167251`. The UI repair branch is separately at `727e57c`; neither branch is an ancestor of the TEST revision. Therefore the compiled and tested application is the pre-feature implementation and cannot pass the ticket acceptance criteria.

## Blocking failures

### 1. SSR first-page and continuation metadata are absent

Repro:

1. Inspect `app/deals/page.tsx` at the tested revision.
2. Observe that SSR fetches exactly `HOTEL_DEAL_PAGE_SIZE` rows and passes only `initialDeals` to `DealFeed`.
3. Inspect `DealFeed` initialization.
4. Observe that it infers `hasMore` from `initialDeals.length === HOTEL_DEAL_PAGE_SIZE` and initializes offset to zero.

Evidence:

- `app/deals/page.tsx:47-56,84-89`
- `app/deals/DealFeed.tsx:277-305`

Expected: SSR uses a trustworthy lookahead/boundary contract and passes server-authored `hasMore`, `nextOffset`, and coverage aligned to the delivered first page.

Actual: no SSR page metadata exists; coverage is inferred from array length.

### 2. `/api/deals` does not return explicit `hasMore`, `nextOffset`, or coverage

Repro:

1. Call `GET /api/deals?limit=12&offset=0` with a mocked 12-row provider result.
2. Inspect the JSON response.

Evidence:

- `app/api/deals/route.ts:108-109,140-164`

Expected: `{ page: { hasMore, nextOffset }, coverage }`, derived using a trustworthy server boundary.

Actual: the API returns `{ deals, total, premium }`; `total` is only current-page length.

### 3. Stable-ID deduplication and continuation integrity are absent

Repro:

1. Return a continuation page containing an ID already present in the SSR page.
2. Trigger continuation.
3. Observe that the duplicate is appended and counted/rendered again.

Evidence:

- `app/deals/DealFeed.tsx:384-388`
- `lib/deals/feedContract.ts:1-3`

Expected: deduplicate by stable `deal.id` before append/counting and advance using server-authored continuation metadata.

Actual: continuation concatenates arrays directly; the shared contract contains only the page-size constant.

### 4. Initial and continuation failures are not separate; retry cannot reuse the failed offset

Repro:

1. Load a settled first page.
2. Cause the next `/api/deals` request to return a non-2xx response.
3. Observe the global `error` state and full-feed error treatment.
4. Activate Retry.

Evidence:

- `app/deals/DealFeed.tsx:338-348,412-422`
- `app/deals/DealFeed.tsx:615-619`

Expected: retain existing cards, show a local continuation failure, and retry the same failed server offset.

Actual: one global error state handles both failure classes; continuation uses locally calculated `offset + 12`, and no failed-offset retry state exists.

### 5. Normalized `HotelProvider` coverage metadata is absent

Repro:

1. Inspect the shared `HotelProvider` contract and HotelLook adapter.
2. Return either 20 offers or zero offers from HotelLook.

Evidence:

- `lib/types.ts:290-298`
- `lib/providers/hotellook.ts:421-458,504-505`

Expected: `Result<HotelSearchPage>` with normalized `offers` and explicit `coverage`, preserving unconfirmed coverage for the capped HotelLook response.

Actual: `Result<HotelOffer[]>`; the adapter exposes no normalized coverage metadata.

### 6. Required boundary UI, responsive states, and accessibility behavior are absent

The tested revision has no `ResultCoverageBoundary` component, native manual continuation control, persistent polite coverage live region, confirmed-end/zero-new copy, or local retry wrapper. Therefore the 375px full-width controls, 1280px grid-spanning boundary, stable keyboard focus, Enter/Space behavior, and transition announcements cannot be verified and do not meet the design acceptance criteria.

Repository search also found no tests for `ResultCoverageBoundary`, `nextOffset`, `continuation_failed`, `zero_new_unconfirmed`, or `HotelSearchPage` at this revision.

## Automated checks

- `npx tsc --noEmit --incremental false`: **PASS**, exit 0.
- `npm test -- --passWithNoTests`: **PASS**, exit 0; 42 suites, 348 tests. Jest reported a worker that did not exit gracefully and force-exited, but the command returned 0.

These checks validate the pre-feature revision only and do not cover the ticket's required state machine or contracts.

## Out-of-scope / handoff findings

- The assigned DEV commit `0167251` and UI repair commit `727e57c` are on divergent branches and are not integrated into this TEST worktree. QA did not cherry-pick or modify feature code.
- Read-only inspection of the DEV branch shows `buildDealPage` computes `nextOffset` from the post-deduped item count. The design edge-case rule requires advancing from the server-consumed offset/token rather than the post-deduped length. This should receive a dedicated partial-duplicate test when the implementation is integrated.
- Full interactive viewport and assistive-technology validation is blocked until the intended implementation is present in the assigned TEST worktree.
